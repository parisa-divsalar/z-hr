import { Stack, Typography } from '@mui/material';

const Step3 = () => {
  return (
    <Stack alignItems='center' height='100%' p={2}>
      <Typography>CV Preview</Typography>
    </Stack>
  );
};

export default Step3;
