# Admin panel: URL of the main Z-HR app (where APIs and DB run).
# Copy this file to .env.local and set the value. Then run: npm run dev
NEXT_PUBLIC_ZHR_API_URL=http://localhost:3000

# Note: RAPIDAPI_KEY and other API keys stay in the MAIN project .env.local (root),
# not here. This panel only calls the main app's APIs (e.g. /api/admin/database, /api/learning-hub/sync).
